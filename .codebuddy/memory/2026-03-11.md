# 2026-03-11 工作日志

## KarvisForYou 部署配置

### 完成的步骤
1. ✅ Python 3.9.6 环境检查通过
2. ✅ requirements.txt 依赖安装完成
3. ✅ `.env` 环境变量配置（DeepSeek + 企微）
4. ✅ Lite 本地模式（OneDrive 未配置）
5. ✅ cloudflared 内网穿透安装（`/tmp/cloudflared`，需用 `--protocol http2`）
6. ✅ Karvis 服务启动成功

### 关键配置
- DeepSeek 模型：`deepseek-chat`（原 `deepseek-v3-0324` 不存在已修正）
- 企微 Corp ID: `wwdab8ed1464222a76`，Agent ID: `1000002`
- 默认用户: `LiangHaoMing`
- 存储模式: Lite 本地（笔记在 `data/users/LiangHaoMing/`）
- cloudflared 必须用 `--protocol http2`（QUIC 在当前网络超时）

### 遇到的问题及解决
- setup.sh 有 Windows 换行符 → `sed -i '' 's/\r$//' setup.sh`
- DeepSeek 模型名错误 → 改为 `deepseek-chat`
- 企微可信 IP 未配置 → 添加 `59.37.125.58`
- cloudflared QUIC 超时 → 使用 `--protocol http2`
- 隧道 URL 每次重启会变，需同步更新 `.env` 和企微后台回调 URL
- `.env` 中重复配置项导致替换遗漏 → 需仔细检查

### 云服务器部署（已完成）
- 腾讯云轻量应用服务器，公网 IP: `49.235.107.213`
- Docker CE 镜像，Docker 27.5.1 + Docker Compose v2.32.4
- 使用 Docker Compose 方式部署（`/root/KarvisForYou/deploy/`）
- GitHub 镜像加速克隆: `ghfast.top`
- `.env` 关键变更: `WEB_DOMAIN=49.235.107.213:9000`, `PROCESS_ENDPOINT_URL=http://127.0.0.1:9000/process`
- `ADMIN_TOKEN=karvis-admin-2026-xK9mPqR7`
- 防火墙已开放 TCP 9000 端口
- 企微回调 URL: `http://49.235.107.213:9000/wework`
- 企业可信 IP: `49.235.107.213`
- 部署测试通过，消息收发正常

### 待办
- 用户想用 Obsidian + 坚果云方案同步笔记（不想用 OneDrive）

### Gemini 模型接入（已完成代码修改）
- 修改文件: `config.py`、`brain.py`、`.env`、`.env.example`、`web_routes.py`
- 新增 `_call_gemini()` 函数，兼容 OpenAI API 格式
- 模型路由逻辑: 配置 GEMINI_API_KEY 后自动替代 DeepSeek 作为 main 层
- 降级策略: Gemini 失败 → DeepSeek → None
- 成本估算: Gemini 输入 ¥0.5/百万 token，输出 ¥2/百万 token
- 默认模型: `gemini-2.5-flash`
- 默认 BASE_URL: `https://generativelanguage.googleapis.com/v1beta/openai`
- 国内服务器需要代理中转才能访问 Google API
- 代码已改好，用户需填入 GEMINI_API_KEY 并重新部署

### Git 仓库独立化（已完成）
- 原仓库: `sameencai/KarvisForYou` → 改名为 `upstream`
- 用户自己的仓库: `git@github.com:Homing-tech/my-karvis.git` → 设为 `origin`
- SSH 密钥: `~/.ssh/id_ed25519`，已添加到 GitHub
- Git 身份: `Homing-tech` / `Homing-tech@users.noreply.github.com`（仅项目级别）
- 首次提交: `feat: 接入 Gemini 模型支持` (51159a8)
- 服务器还未切换到新仓库，需要在腾讯云上执行 `git remote set-url origin git@github.com:Homing-tech/my-karvis.git`
